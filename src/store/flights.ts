// import { TFlights } from '@/lib/types';
// import { create } from 'zustand';

// type FlightsStore = {
//   flights: TFlights | null;
//   setJourney: (journey: Partial<TJourney> | null) => void;
// };

// const useJourneyStore = create<JourneyStore>((set) => ({
//   journey: null,
//   setJourney: (journey) =>
//     set((state) => ({ journey: { ...state.journey, ...journey } }))
// }));

// export { useJourneyStore };
